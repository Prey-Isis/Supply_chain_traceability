server:
  port: 8080
  mode: debug

database:
  driver: mysql
  host: localhost
  port: 3306
  username: root
  password: "123456"
  database: supply_chain

# RabbitMQ 消息队列配置
# 本地开发：host 用 localhost（通过 docker run 或 docker-compose 起 MQ）
# Docker 部署：host 用 rabbitmq（compose 服务名），由 start.sh 环境变量覆盖
rabbitmq:
  host: localhost
  port: 5672
  username: supply_mq
  password: "SupplyMQ@2024"
  vhost: "/"
